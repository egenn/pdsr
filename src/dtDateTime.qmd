# IDate and ITime {sec-dtdatetime}

The data.table package adds the `IDate` and `ITime` classes that store date and time information, respectively, using integers instead of double precision floating point format. This allows fast sorting and grouping.