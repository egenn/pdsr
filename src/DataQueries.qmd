# Data Queries {#data-queries}

In this section, we look at common data operations, including:

- Filtering, i.e. identifying a subset of cases (rows)
- Selecting, i.e. identifying a subset of variables (columns)
- Sorting, i.e. ordering cases (rows) by the values of a variable

how to query data. We will look at the following:

Querying data is often used in the context of a database. Here, we look at common
operations on tabular data, whether 